permissions:
  contents: write

on:
  push:
    branches: [main]

jobs:
  sync-staging:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: |
          git push origin main:staging
